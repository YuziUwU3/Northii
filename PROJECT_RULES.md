# 小手机项目规则与当前目标

## 2026-08-16 微信统一自然系统

1. “微信自然模式”测试按钮已经退役，网页与私人 App 以后默认使用同一套统一系统，不再维护开／关两条分叉路线。
2. 统一系统保留小号身份隔离与主号一次性报备、主动消息与电话、节日／日程／步数／电量等生活数据、心情与关系连续性、忽略反应、外部事件和行为证据。
3. 恢复能力必须保持自然：依赖真实上下文和人物性格，并保留冷却、证据门槛、明确拒绝、睡眠／工作边界和最多次数限制，禁止机械查岗、固定台词与重复骚扰。
4. 长期记仇账本和强制权力／惩罚模板不并入默认系统；除非用户以后明确要求，不要重新打开。
5. 新增或修复微信功能时不得再用 `wechatNaturalOn()` 提前返回、跳过记录或截断主动事件；内部兼容函数可以保留，但不得重新出现用户开关。

## 当前唯一目标

现在只做一件事：

做一个“小手机快捷指令/后台唤醒机制”：

当用户长时间没有打开小手机网页时，小手机能自动被唤起或自动进入一次检测流程，触发角色消息/提醒，而不是必须用户手动打开。

重点目标不是抖音，不是 GitHub Pages，不是重新部署旧东西，而是：

确认“小手机久未打开后，是否能自动触发 phone_idle / 角色消息”。

---

## 已排除：不要再重复

以下方案已经试过或方向不对，没有新依据不要再让我重复：

1. 不要再让我反复 Push GitHub
原因：本地 HEAD 和 origin/main 已确认一致，Push 不是当前问题。

2. 不要再让我重新建 Supabase 项目
原因：旧项目可用，数据库和 Edge Function 已经存在。

3. 不要再让我重复测试同一个 webapp://...#event=phone_idle 链接
原因：昨天已经试过，结果不稳定。有时成功，有时没消息。没有新变量不要重测。

3a. 不要再让我重复测试 webapp://...?event=phone_idle 链接
原因：2026-07-09 已实测，能打开主屏小手机，但调试面板收到时间仍停留在上一次模拟，说明 query 事件也没有传进主屏 webapp。

4. 不要继续做抖音方案
原因：方向不对，不做。

5. 不要走“靠通知让我手动点”的方案
原因：我要的是自动触发，不是手动点通知。

6. 不要继续绕回“等待 + 打开 URL”这种旧流程
原因：已经试过，结果不稳定。

---

## 已验证但不稳定

1. 自动跳转：成功过，但不稳定。
2. 跳转后角色消息：成功过一次，但多次没有消息。
3. 小手机本体已有诊断能力，但信息还不够判断卡在哪一步。

## 本次新增验证

1. v379 本地模拟已验证：小手机本体可以收到模拟 phone_idle，进入处理逻辑，选中角色，打开聊天页。
2. v379 本地错误路径已验证：聊天 API 未配置时，调试面板会显示“API配置/权限”，不会误判成快捷指令问题。
3. v379 本地成功路径已验证：AI 返回正常内容时，调试面板会显示“已生成角色消息”，聊天里会出现角色消息。
4. v379 手机实测已验证：在设置页点击“模拟久未打开超过1分钟”后，角色成功发消息。说明小手机本体收到 phone_idle 后的内部链路已经打通。
5. v379 手机实测已验证：快捷指令用 webapp://...#event=phone_idle 可以打开主屏小手机，但调试面板“收到 phone_idle”仍停留在上一次模拟时间，说明 hash 事件没有传进主屏 webapp。
6. v379 手机实测已验证：快捷指令用 webapp://...?event=phone_idle 也可以打开主屏小手机，但调试面板“收到 phone_idle”仍停留在上一次模拟时间，说明 query 事件同样没有传进主屏 webapp。
7. v380 本地浏览器已验证：打开 phone-idle-local.html 后，页面能写入 localStorage 待处理 phone_idle，随后主页面能消费该事件并进入处理逻辑。
8. v380 手机实测已排除：快捷指令打开 webapp://.../phone-idle-local.html 后，主屏小手机能打开，但调试面板“收到 phone_idle”时间没有变化。说明 iPhone 主屏 webapp 没有稳定执行这个本地桥页面，不能继续重复测这一条。
9. v380 手机实测云端收件箱第一次失败：12:48 运行“先 POST phone_external_events，再打开主屏小手机”后，调试面板“收到 phone_idle”没有变化；随后电脑查询 Supabase，目标 yb_pelf2zn3tlgool48abeeiemr5d1h2c 没有 12:48 新记录，说明问题在快捷指令 POST 没成功，不是小手机内部处理失败。
10. v380 手机实测：删除请求体里的 payload，只保留 target/event 后再次运行，调试面板仍未变化；电脑查询 Supabase 仍没有新记录。说明当前重点不是小手机取事件，而是快捷指令的“获取 URL 内容”没有成功写入云端或没有按预期执行。
11. v380 电脑诊断已验证：电脑向同一 target POST phone_idle 到 phone_external_events 成功；随后用户手动打开小手机，调试面板“收到 phone_idle”时间更新。说明小手机云端收件箱轮询正常，剩余问题只在 iPhone 快捷指令 POST 写入方式。
12. v380 手机快捷指令诊断：添加“显示结果”后抓到错误 No API key found，说明 iPhone 快捷指令没有把 apikey 头部成功发出；把 apikey 改放到 URL 参数后，错误变为 PGRST204 Could not find the 'event ' column，说明 API key 已生效，剩余问题是请求体字段名 event 后面多了空格。
13. v380 手机快捷指令诊断：删除重建 event 字段后，错误变为 PGRST204 Could not find the 'target ' column，说明 event 字段已修好，target 字段名后面也有隐藏空格，需要同样删除重建 target 字段。
14. v380 手机实测成功：清理请求体字段名隐藏空格后，快捷指令成功 POST 到 phone_external_events；电脑确认云端出现 13:06 新记录；用户点“完成”继续打开主屏小手机后，设置页“久未打开调试”的“收到 phone_idle”时间成功更新。当前可用链路为：快捷指令先 POST 云端事件，再打开主屏小手机，小手机轮询云端收件箱处理 phone_idle。
15. v380 手机实测成功：删除临时“显示结果”后再次运行快捷指令，无弹窗，直接打开主屏小手机；小手机成功收到 phone_idle，角色自动跳到微信并发送消息。当前核心链路已可用。
16. v380 手机自动化实测成功：用“打开抖音时”自动化运行成功快捷指令，可以自动跳到主屏小手机并触发角色微信消息；在自动化内加入“等待 60 秒”后，打开抖音约 1 分钟后自动跳小手机并发消息成功。当前待解决问题：多次打开触发 App 可能启动多个并行倒计时，需要加计时锁防重复。
17. 旧抖音方案可复用的部分：本机文件 lock 防重复计时。流程是检查“我的 iPhone/douyin_lock.txt”，存在且有内容就停止；不存在就写入 lock 文件，等待测试时间，结束后打开小手机。新方案只复用 lock 思路，lock 文件应改名为 phone_idle_lock.txt，不再使用 douyin_timeout 链接。
18. v380 手机计时锁调试：phone_idle_lock.txt 已能写入，文件显示 4 字节，说明落锁成功。手动连续点快捷指令播放时，第二次无法点击，是因为第一轮快捷指令仍在“等待 60 秒”执行中；这个现象不能单独证明 lock 防重复，需要用自动化连续触发测试。
19. v380 手机计时锁调试：等待结束后已成功跳小手机一次，但 phone_idle_lock 文件仍保留 4 字节 lock，导致第二次不会再跳。说明落锁成功、阻止重复成功，但清锁失败；下一步只修“删除 phone_idle_lock”这一步。
20. v380 手机自动化闭环成功：修正清锁步骤为“等待后重新从路径获取 phone_idle_lock.txt，再删除该文件，立即删除开启”。实测自动化成功触发，等待 60 秒后自动跳小手机并发消息，结束后 phone_idle_lock 文件不存在。当前 1 分钟测试版闭环已通过。
21. v380 多 App 自动化实测暴露问题：选择 3 个 App 后快速依次打开，每个 App 似乎都会启动 60 秒计时，之后反复出现删除 phone_idle_lock 文件的动作/提示。说明“删除文件清锁 + 多 App 快速触发”仍可能产生多个并行等待队列；下一步不要继续扩大 App 数量，应改成更稳的锁方案，避免删除确认和并发清锁。
22. v380 重要排除：在没有改动自动化结构之前，不要再让用户重复测试“同一个自动化里多选多个 App”。该方案已验证会产生多个并行倒计时/清锁提示，没有新变量前禁止重测。
23. v380 多自动化实测也失败：拆成多个 App 单独自动化、都运行同一个快捷指令后，仍会出现重复倒计时和重复删除文件提示。说明问题不只在“同一个自动化多选 App”，而是删除文件清锁方案本身不适合多触发并发场景。下一步必须去掉“删除文件”，改成固定 lock 文件存在、通过内容 lock/空 来判断。
24. v380 清空锁文件实测：将“删除 phone_idle_lock”改成“空文本覆盖保存到 phone_idle_lock.txt”后，自动化到点能跳小手机，锁文件保留但显示 0KB，说明清锁成功且不再需要删除文件。该次测试角色未回复，下一步应查看小手机“久未打开调试”确认 phone_idle 是否收到、时间判断是否被“刚打开过”拦住，或 AI/生成链路是否异常。
25. v381 代码修复：手机实测显示收到 phone_idle 时“上次打开小手机”和事件时间同一分钟，导致时间判断为“未触发：离开 0分钟”。原因是打开小手机本身刷新了最后打开时间。v381 已在 idleTouchOpen() 刷新前保存上一轮打开时间，并在 handleIdleEvent() 遇到“当前打开刚刷新导致不足阈值”时，用打开前的时间补判；调试面板新增“判断用的时间”。
26. v381 手机实测成功：v381 推送后，自动化等待 60 秒跳小手机，角色成功发消息。说明“打开小手机导致离开时间变 0 分钟”的代码问题已修复。
27. 快捷指令锁结构当前状态：不要再尝试“获取文件内容 → 如果内容是 lock”，因为 iPhone 快捷指令把它识别成文件/文件夹内容，条件不好设置。当前采用文件大小判断：phone_idle_lock.txt 为 0KB 表示未计时，写入 lock 后约 4 字节表示正在计时。
28. 当前快捷指令正确结构：开头为“从路径 phone_idle_lock.txt 获取文件，未找到时报错关闭 → 如果 文件大小 大于 0 MB → 停止执行；否则 → 文本 lock → 保存到 phone_idle_lock.txt 覆盖 → 等待 60 秒 → 空文本 → 保存到 phone_idle_lock.txt 覆盖 → 获取 URL 内容 POST phone_idle → 打开 webapp 小手机 → 结束如果”。不要再改成内容判断，除非有新证据。
29. 单 App 已确认稳定，不需要再要求用户重复验证。当前唯一剩余目标是验证/修正多个 App 触发时是否只认第一次，不产生多个并行倒计时。
30. 当前截图确认的快捷指令结构是正确方向：phone_idle_lock.txt 若大于 0 MB 就停止；否则写入 lock、等待、用空文本覆盖成 0KB、POST phone_idle、打开小手机。下一次多 App 测试必须说明新变量：相较上次失败，已经从“删除 lock 文件”改成“空文本覆盖成 0KB”，不再触发删除弹窗。
31. 用户确认：即使改成空文本覆盖 0KB，选择 2 个 App 时仍会重复计时两个。说明本机文件大小锁仍挡不住 iPhone 多个 App 自动化并发启动。不要再要求原样测试“多个 App + phone_idle_lock 文件大小锁”。
32. v382 云端锁已验证：Supabase SQL 执行成功；电脑调用 `phone_idle_try_lock` 返回 `ok`；手机快捷指令清掉空头部后，第一次调用显示 `OK`，立刻第二次调用显示 `LOCKED`。说明云端锁可以做到“第一轮占锁，后续触发直接拦住”。下一步应把云端锁接到原来的主快捷指令开头，不再依赖本地 `phone_idle_lock.txt` 防多 App 并发。
33. v382 快捷指令条件限制：手机里 `如果 URL 的内容` 只能选“有任何值/没有值”，不好直接判断 `locked` 文本。因此云端锁函数调整为：抢锁成功返回 `ok`，已有锁时返回空值/null。快捷指令应使用“如果 URL 的内容 没有值 → 停止执行；否则继续”。
34. v382 云端锁已接入主快捷指令开头：结构为先调用 `phone_idle_try_lock`，如果 `URL 的内容 没有值` 就停止；否则继续原流程。手机手动播放主快捷指令时已进入“等待 60 秒”，说明云端锁放行成功。但本次等待结束后没有跳到小手机。下一步不要重复测试播放；先检查“等待 60 秒”后面的动作是否还存在：空文本、覆盖保存 0KB、POST `phone_external_events`、打开 `webapp://...小手机.html`。
35. v382 主快捷指令恢复成功：确认后半段被误删后，只恢复必要动作，不恢复本地文件锁。当前有效结构为：云端锁 → 若无返回值则停止 → 文本 lock/保存（可保留但不承担多 App 防重）→ 等待 60 秒 → POST `phone_external_events` 的 `phone_idle` → 打开主屏小手机。用户手动播放后，角色成功发消息，说明云端锁版本的单次完整闭环已恢复。下一步可以进入多 App 测试，但只测一次，不再修改本地文件锁。
36. v382 多 App 最终验证成功：自动化里选择 2 个 App 后，用户依次打开两个 App，最终只跳转小手机 1 次。说明云端锁已经实现“多个 App 触发时只认第一次，后续触发作废”，当前测试目标达成。后续不要再回到本地 `phone_idle_lock.txt` 防重方案。

---

## 下一步应该做什么

小手机本体链路已经验证通过。

下一步只做一件事：

解决“多个 App 触发时只认第一次，不产生多个并行倒计时”。

要求：

1. 不再重复测试小手机内部模拟按钮，除非后续改动影响了它。
2. 不再走抖音方案。
3. 直达方案已有新证据证明不可行：hash、query、本地桥页面在手机主屏 webapp 中都没有稳定把事件传进去。
4. 下一步改为“云端事件收件箱 + 打开主屏小手机”：快捷指令先写入 phone_external_events，再打开主屏小手机，由小手机自己轮询云端事件并触发 phone_idle。
5. 每次只让用户做一个明确操作，并说明这一步和昨天已失败步骤的区别。
6. hash 和 query 都已排除，下一步不要再靠 URL 参数传事件。
7. 不再重复测试“专用本地桥页面/路径触发”，因为手机实测已经失败。
8. 云端事件收件箱链路已成功，不再重复调试 POST/打开小手机链路，除非后续改动影响它。
9. 同一个自动化里多选多个 App 已排除，不要原样重测。
10. 下一步要改的是快捷指令锁结构：让第一轮触发后立刻落锁，后续触发只停止，不进入等待；并避免删除文件弹窗/并发清锁。

---

## AI 必须遵守的工作规则

1. 每提出一步操作前，必须检查这一步是不是已经试过。
2. 如果试过，必须说明这次和上次有什么本质不同。
3. 如果没有新变量，禁止让我重复测试。
4. 每完成一次测试，必须更新“已验证/已排除清单”。
5. 不要只说“你说得对”。如果同意，必须说明这会如何改变后续计划。
6. 不要一次给我一大堆方案。一次只给我一个明确操作。
7. 我不是工程师，步骤必须具体到“点哪里、看什么、拍什么截图”。
8. 不要让我反复操作 GitHub、Supabase、Pages、Push，除非有明确新证据。
9. 如果你不确定，先问我拿截图，不要猜。
10. 当前目标完成前，不要扩展别的新功能。

---

## 当前判断

小手机内部问题已经基本排除。

现在问题范围缩小为：

iPhone 快捷指令云端事件链路已经成功；单 App 1 分钟自动化闭环成功；同一个自动化里多选多个 App 已失败。当前问题只剩：多个 App 触发时的并发倒计时。下一步必须先改锁结构或自动化拆分方式，不能原样重复多 App 测试。

## 2026-07-09 新决定：多 App 只认第一次

当前目标不是继续试本地文件锁，而是升级为云端锁：

1. 任意一个 App 第一次触发时，先请求 Supabase 云端锁。
2. 云端返回 `ok`，才允许进入等待 60 秒 / 正式 1 小时。
3. 等待期间，其他 App 再触发会拿到 `locked`，必须直接停止。
4. 到时间后，快捷指令继续写入 `phone_external_events`，再打开主屏幕小手机。
5. 云端锁设置过期时间，避免异常卡死。

已新增文件：

`supabase_idle_lock.sql`

下一步只做一件事：

先在 Supabase SQL Editor 执行 `supabase_idle_lock.sql`，建立 `phone_idle_locks` 和 `phone_idle_try_lock(...)`。

执行成功前，不要继续改快捷指令多 App 结构。

不要再重复测试：

- 多个 App + `phone_idle_lock.txt` 文件大小锁
- 多个 App + 删除文件清锁
- 多个 App + 0KB 空文件清锁
- “获取文件内容是 lock” 条件判断
- 临时“云端锁”单独测试 OK/LOCKED，已通过，不要重复

## 2026-07-09 严重失误记录

本次指导中，AI 在用户发出主快捷指令截图后，只检查了上半段云端锁结构，没有确认 `等待 60 秒` 后面是否还保留 `POST phone_idle` 和 `打开小手机` 动作，就错误判断“结构正确”。随后用户按指导删除本地锁相关动作时，后半段必要动作也被删掉，导致快捷指令只会等待，不会再发事件或打开小手机。

以后必须遵守：

1. 用户发快捷指令截图时，必须先确认完整流程是否仍包含“等待后发事件、打开小手机”。
2. 让用户删除动作前，必须明确说“只删哪几块、保留哪几块”，不能只说大概。
3. 如果截图没有显示完整流程，不能判断“正确”，必须要求继续往下拍。
4. 当前恢复方案：不恢复本地文件锁，只补回云端锁后面的必要动作：`等待 60 秒` → `获取 URL 内容 POST phone_idle` → `打开小手机`。

## 2026-07-09 新目标：强制聊天模式

久未打开自动跳回已经验证成功：多 App 触发时只跳一次。

下一步改小手机本体：

1. phone_idle 触发后，角色要知道是自己把用户强制拉回了微信聊天页。
2. 跳回后 10 分钟内，小手机网页内部不允许正常退出聊天页。
3. 聊天页显示一个“小洞”入口，用户可以从小洞逃走。
4. 如果用户从小洞逃走，角色会知道，并按自己的性格更生气/委屈/强势/危险地反应。
5. 这个锁只能控制小手机网页内部，不能控制 iPhone 系统级上滑退出或杀后台。
6. 不要再改快捷指令锁结构，除非后续测试证明网页内部处理成功后还有问题。

## 2026-07-09 强制聊天模式追加规则

1. iPhone 网页不能真正锁死系统级退出、上滑回桌面、杀后台，也不能阻止用户离开小手机 App。
2. 小手机能做到的是：10 分钟内只要用户回到小手机页面，就继续拉回对应角色聊天页。
3. 如果用户没有走“小洞”，而是直接离开小手机，回到页面后角色会知道这次绕开行为，并按性格自然反应。
4. 角色可以提前放行：AI 单独输出 `[放行]` 时，小手机解除这次 10 分钟停留。
5. 角色和界面都不要使用突兀词：系统、网页、按钮、快捷指令、后台、锁死、强制、扣住、把你扣这儿了。
6. 提醒话术必须自然且减少重复，不要每次都从“太久没理我”开头。

## 2026-07-09 新决定：取消强制停留

用户决定去掉“强制十分钟留在聊天页 / 小洞逃走”这套网页内停留机制。

保留的目标：

1. 多 App 触发只认第一次。
2. 到时间后打开主屏幕小手机。
3. 小手机收到 phone_idle 后跳到角色微信聊天页。
4. 角色按性格自然发消息，且不要重复、不要机械。

已取消：

1. 不再强制 10 分钟停留。
2. 不再显示“小洞”。
3. 不再阻止小手机内部返回/回首页。
4. 设置页不再展示“久未打开调试”面板。

正式时间注意：

1. 小手机内部阈值固定按至少 1 小时判断，避免旧的 1 分钟测试值残留。
2. 快捷指令正式使用时，“等待”要改成 3600 秒。
3. 云端锁 `p_ttl_seconds` 要大于等待时间，建议 3900 或 4000 秒，避免 1 小时内再次开 App 重新起计时。

## 2026-07-09 前台刷新与语音缓存

本次继续只改小手机本体，不改快捷指令结构。

新增目标：

1. 小手机网页在前台可见时，每 60 秒刷新一次“最后打开小手机时间”。
2. 小手机退后台 / 页面隐藏时，停止这个刷新。
3. 这样用户正在小手机里聊天时，不会被久未打开机制误判。
4. 语音标签识别要更稳：除了 `[语音|内容]`，也兼容 `【语音｜内容】`、`[语音：内容]` 等常见写法。
5. 角色已经生成过的语音要可重复点击播放。
6. 重复播放旧语音只读本地缓存，不再调用 AI 账户 TTS，不再扣点，也不再重新请求海螺 / MiniMax。

注意：

1. 旧版本已经生成但没有保存音频缓存的语音，第一次重新点可能仍需要生成一次；生成成功后才会缓存。
2. 以后不要把“重复播放旧语音”当成新的 TTS 请求处理。

## 2026-07-09 内置语音模型与字数上限

本次只限制“小手机 AI 账户 / phone-ai 内置语音”：

1. 内置 AI 账户语音固定使用 `speech-02-turbo`，避免测试用户误选 `speech-02-hd` 等更贵模型。
2. 设置页里用户自己填写的外部海螺 / MiniMax / 其他 TTS 配置不受影响，外部自配模型仍按用户自己填写的值走。
3. 单条语音内容最多 300 字。
4. 角色输出超过 300 字的语音标签时，小手机前端要把它当普通文字消息处理，不生成语音、不扣 TTS 点数。
5. phone-ai 后端也要做 300 字保护，与前端语音上限一致，防止绕过前端直接请求长语音。
6. 内置 TTS 必须“成功后扣点”：先检查点数够不够，再请求海螺；只有拿到可用音频后才写小手机扣点流水。海螺失败、没音频、音色无权限、海螺余额不足时，小手机 AI 点数不扣，不再走“先扣再退”。
7. 内置 TTS 按实际语音正文每 50 字扣 1 点并向上取整：1～50 字 1 点，最多 300 字 / 6 点；余额预检、最终扣点和流水记录必须使用同一个服务端计算结果。

## 私人 App 交付包死规则（2026-08-15）

1. 每次只交付当前最新版本的一个 ZIP，交付目录里不得同时留下同版本中间包、旧 ZIP 或旧交付物。
2. ZIP 必须保留编译当前新版所必需的完整 Xcode 工程与资源，不能做成无法独立编译的零散补丁包。
3. ZIP 只保留“请在Mac编译前先读.md”和本次最新安装说明；严禁夹带历次安装说明。
4. 严禁把预览图、测试缓存、`.codex_tmp`、`__pycache__`、`.pyc`、临时脚本或其他工作现场打进交付包。
5. 打包完成后必须检查：只有一个内置 `PhoneWeb.bundle/index.html`、没有嵌套 ZIP、版本号一致，并列出实际文件数量与校验值。

## iPhone 主屏幕底部系统条停止修改（2026-08-15）

1. 用户确认该区域按 iOS 系统自带区域处理，不再把它列为网页缺陷继续修复。
2. 保持 v934 已有处理；禁止再尝试改色、消除、扩展 viewport、拉伸页面、整体上下位移或借“苹果兼容”开关继续调整该区域。
3. 后续输入栏、锁屏、主页或 Android 适配不得顺带修改这条系统区域；除非用户以后重新明确授权，否则不再动它。
