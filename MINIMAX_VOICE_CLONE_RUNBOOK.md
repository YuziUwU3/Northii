# MiniMax 音色克隆成功条件

文档修订：`v585-doc1`

最后验证日期：2026-07-20

本文记录已经真实验证成功的 MiniMax / 海螺音色克隆链路。新聊天在继续排查或执行克隆前，必须先阅读本文，避免重复走已经失败的旧路径。

## 已验证成功的必要条件

### 1. 上传和克隆必须使用同一个域名

统一使用中国区官方 API 域名：

```text
https://api.minimaxi.com
```

已验证成功的两个请求地址：

```text
POST https://api.minimaxi.com/v1/files/upload?GroupId=<TEAM_ID>
POST https://api.minimaxi.com/v1/voice_clone?GroupId=<TEAM_ID>
```

不要把上传请求发到 `api.minimax.chat`，再把克隆请求发到 `api.minimaxi.com`，也不要反过来混用。跨域名链路可能出现“上传成功，但克隆返回 `2013 invalid params`”的假象。

### 2. API Key 和 GroupId 必须属于同一个团队

- `Authorization` 请求头使用 `Bearer <API_KEY>`。
- URL 中使用与该密钥对应的 `GroupId`。
- 不要把一个账号的 API Key 和另一个账号的团队 ID 拼在一起。
- 密钥只在本次运行的环境变量中使用，不得写入代码、文档、日志或提交记录。

### 3. 上传参数必须正确

上传接口使用 `multipart/form-data`：

```text
purpose=voice_clone
file=<音频文件>
```

克隆原音频要求：

- 格式：`mp3`、`m4a` 或 `wav`
- 时长：10 秒至 5 分钟
- 大小：不超过 20 MB

必须先确认上传响应中的：

```json
{
  "base_resp": {
    "status_code": 0,
    "status_msg": "success"
  },
  "file": {
    "file_id": 123456789
  }
}
```

只把本次同域名上传返回的 `file_id` 交给克隆接口，不复用其他域名或其他账号上传得到的 `file_id`。

### 4. voice_id 必须符合规则

- 长度为 8 至 256 个字符。
- 首字符必须是英文字母。
- 只能包含字母、数字、`-`、`_`。
- 末尾不能是 `-` 或 `_`。
- 必须是未被占用的新 ID。

例如：

```text
gushiye01
```

`gushiye` 只有 7 位，不符合要求。返回 `2039` 表示该 ID 已存在，不代表又成功克隆了一次；应换一个新 ID 后重试。

### 5. 克隆先使用最小请求体

已验证成功的最小请求体：

```json
{
  "file_id": 123456789,
  "voice_id": "gushiye01"
}
```

请求头：

```text
Authorization: Bearer <API_KEY>
Content-Type: application/json
```

只有需要试听时才增加 `text` 和 `model`。不要在基础链路尚未成功时堆叠 `clone_prompt`、ASR 文本校验、降噪等可选参数。

## 成功判断

真正成功必须同时满足：

```json
{
  "base_resp": {
    "status_code": 0,
    "status_msg": "success"
  }
}
```

HTTP 状态为 `200` 不等于业务成功，必须继续检查 `base_resp.status_code`。

## 常见错误定位

| 错误码 | 含义 | 处理 |
| --- | --- | --- |
| `0` | 成功 | 保存最终 `voice_id` |
| `1008` | 余额不足 | 检查账户余额或赠送额度 |
| `2013` | 请求参数错误 | 优先检查是否混用了域名、文件 ID、API Key/GroupId |
| `20132` | 克隆样本或 voice_id 错误 | 检查音频与 voice_id 规则 |
| `2037` | 原音频时长不合规 | 调整到 10 秒至 5 分钟 |
| `2038` | 克隆功能被禁用 | 检查个人或企业认证状态 |
| `2039` | voice_id 已存在 | 更换一个未使用的新 ID |
| `2048` | 示例音频过长 | `prompt_audio` 必须少于 8 秒 |
| `2049` | API Key 无效 | 检查密钥及所用平台域名 |

## 账号与计费注意事项

- 调用克隆接口成功时不会立即收取音色复刻费。
- 克隆音色在首次正式 TTS 合成时才会激活并触发复刻计费或消耗赠送名额。
- 未激活的临时音色需要在 7 天内正式调用一次，否则可能被删除。
- 是否享受“首次免费”取决于账号活动、套餐或赠送额度，不能仅凭接口成功判断。
- 未经用户明确确认，不要为了验证而调用克隆音色进行正式 TTS，避免触发费用。

## 已排除的误判

出现 `2013` 时，不要直接断定：

- API Key 无效：无效密钥通常返回 `2049`。
- 余额不足：余额不足通常返回 `1008`。
- 未认证：克隆权限禁用通常返回 `2038`。
- 音频一定坏了：同一音频在“同域名上传 + 同域名克隆”后已真实成功。

可先用以下只读或低风险检查缩小范围：

1. `get_voice` 能否返回 `status_code: 0`。
2. 系统音色能否完成一条极短 TTS。
3. 用已存在的 voice_id 做对照，若返回 `2039`，说明克隆接口已经正确解析请求。
4. 重新在 `api.minimaxi.com` 上传音频，并立即用该响应的 `file_id` 在同域名克隆。

## 安全要求

- 聊天中出现过的 API Key 应建议用户在操作完成后轮换。
- 最终回复不得重复展示完整 API Key。
- 仓库中只允许保存占位符，例如 `<API_KEY>`、`<TEAM_ID>`。
- 不记录用户或他人的真实团队 ID、UID、手机号和实名认证信息。

