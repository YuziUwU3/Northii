# 第一百二十四次安装｜v1003 回复链路回归修复

版本：网页 v1003；私人 iOS 1.0.124 (124)；原生桥 25。

请把 ZIP 完整解压成全新目录 `SmallPhone_v1003_ReplyPipelineRegressionRepair`，不要覆盖旧工程目录。只打开该目录中的 `PhoneCompanionTest.xcodeproj`。

本版包含 v1001、v1002 的主动伴生、朋友圈、微信交互、聊天入口和屏幕时间完成态修复，并修正新增回复回归：

1. 共同生活真实读取失败后只保留本轮真实结果，不再由 15 秒循环跨重启反复请求模型；旧存档中的自动重试时间会原位迁移为人工重试状态。
2. 真实读取已经占用或在回复期间接管时，普通微信回复正常静默，不再重复请求或错误显示“模型未回复／API 超时”。
3. 朋友圈评论仍只接受真实模型回复；App 关闭或挂起造成的“回复中”会在下次启动改为真实失败和重试入口，绝不补写角色假回复。
4. 屏幕时间“读取完成”横幅仍在 450 毫秒后结束，不再跟随模型请求长期悬浮。
5. 三个聊天入口继续严格分工：右上角三个点进入角色资料，角色头像进入聊天详情，消息气泡进入消息菜单。

Windows 已完成 JavaScript 语法检查、版本一致性检查和 850/850 自动回归。Windows 结果不能替代 Mac 编译、签名和真实 iPhone 验证。

Mac 验收顺序：

1. 在 Xcode 执行 `Product → Clean Build Folder`，选择 `PhoneCompanionTest` Scheme 和真实 iPhone 编译运行。
2. 先测试普通微信连续回复速度，再测试朋友圈评论；确认不会长期停在“回复中”。
3. 在共同生活读取屏幕时间，确认“读取完成”横幅迅速消失；随后立即测试普通微信和朋友圈，确认不会被后台重试拖慢。
4. 完全关闭并重新打开 App，确认旧的朋友圈 pending 状态变成可重试失败，且旧 phoneFactRetry 不再自动请求模型。
5. 最后复测低电、失联、电量位置、心率关心、每日睡眠步数与全部 App 记录、手动解锁发现和远程操控。

当前尚未完成：Mac 编译、Apple 签名和真实 iPhone 验收。若真机仍异常，请保留当时页面、时间、角色、操作顺序及后台记录，不要继续覆盖旧工程。
