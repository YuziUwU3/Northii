# 第一百次安装：v978 全锁实执行与通话记录修复

- 网页核心：v978
- 私人 iOS：1.0.100 (100)
- 原生桥：23

请完整解压到全新目录 `SmallPhone_v978_AllControlCallLog`，不要覆盖任何旧工程。用 Xcode 打开 `PhoneCompanionTest.xcodeproj`，先执行 `Product → Clean Build Folder`，再选择 `PhoneCompanionTest` Scheme 和真实 iPhone 编译安装。设置页应显示“v978 · 全锁实执行与通话记录修复”和“1.0.100 (100)”。

本版补齐三项：用户本轮明确说“全部锁定／解除全锁”且角色给出同方向控制标签时，错误的“仅内置”标签不能再缩小范围；通话记录删除或编辑单句后保留原展开状态和阅读位置；组件透明度偏好会实际覆盖四套图标主题规则，并同步作用于私人 App 第二页的实体玻璃底色。角色拒绝、动作相反、条件句和问句不会被当成全量执行。

Windows 自动测试只能证明源码、内置资源和工程配置一致，不能证明 Apple 签名、Family Controls 权限、DeviceActivity 回执或真机渲染。安装后请至少检查：全部锁定、解除全锁、指定单个 App、操作记录和设备回执；展开较长通话后删除中间一条是否仍停在原位置；四套主题把组件透明度分别调到 0、默认值和 100 时卡片是否有明显变化。
