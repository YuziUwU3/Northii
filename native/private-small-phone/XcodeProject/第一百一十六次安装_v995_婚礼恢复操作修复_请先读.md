# 第一百一十六次安装：v995 婚礼恢复操作修复

- 网页资源：v995
- 私人 iOS：1.0.116 (116)
- 原生桥：24
- 交付日期：2026-08-19

## 本次必须确认

1. 完整解压并打开全新 `SmallPhone_v995_WeddingRecoveryActions` 工程，不能覆盖或继续编译旧 v969、v979、v990、v991、v992、v993、v994 工程。
2. 设置页必须显示“当前已加载：v995 · 婚礼恢复操作修复”和“私人安装包 1.0.116 (116)”。任一项不符都代表打开了旧包。
3. “线下约会”现在是独立的全屏页面；婚礼只放在该页面内部，不再注册或显示主屏幕“婚礼”App。升级启动时会自动清除旧布局里遗留的婚礼快捷方式，不能出现两个相同爱心图标或应用名重叠。
4. 删除聊天中的中式或现代婚礼邀请卡后，只要本机仍保留已经生成的完整六幕，线下约会必须继续显示完整操作区：立即进入、模拟一次且不保存、单独重做某一章、重新生成对应风格婚礼。
5. “模拟一次 · 不保存”必须直接复用已经准备好的六幕，不得因为邀请卡不存在而误启动整场生图；模拟结束仍不得收藏婚书、写入角色记忆、改变夫妻关系或发送婚后消息。
6. “单独重做某一章”必须能通过现存 `preparedId` 找回该场婚礼并只请求所选章节；新图保存前保留旧图，整场重新生成只在用户明确点击对应按钮后发生。
7. 情侣空间只保留婚书和高清影集的查看入口；模拟、单章重做和整场重新生成等操作按钮全部固定在线下约会，不得再次放入情侣空间。
8. v994 的付费出图修复全部保留：婚礼只走普通图片生成接口，返回图片立即缓存；整场仅在确实无图时重试两次，单章每点一次只发起一张图片请求。

## Mac 与真机验收边界

Windows 已完成源码语法、自动回归、网页／私人资源同步和包结构检查，但不能替代 Mac 编译、签名或真实 iPhone 验证。请在 Mac 首次打开后执行 `Product → Clean Build Folder`，使用 `PhoneCompanionTest` Scheme，重点复测删除中式邀请卡后的四个操作按钮，以及模拟和单章重做不会误触发整场生成。
