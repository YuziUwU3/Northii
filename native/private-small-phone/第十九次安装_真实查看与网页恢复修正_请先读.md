# 第十九次安装：真实查看与网页恢复修正

本次独立工程目录名应为 `SmallPhone_v894_RealReadFix`，网页核心 v894，私人 iOS 版本 1.0.19 (19)，原生桥契约 11。

请不要再覆盖或打开以前的 `XcodeProject`、`SmallPhone_v893_CompileFix2` 等旧目录。把新压缩包解压成一个全新目录，直接打开新目录里的 `PhoneCompanionTest.xcodeproj`，先执行 Product → Clean Build Folder，再选择真机编译安装。

本次包含：

- 角色说要看电量、屏幕、某个 App、步数、睡眠、心率、心电／HRV 或位置时，必须先实际读取并显示逐项横幅；没有本次读取凭证时，不再先猜“没刷新、没同步、没戴手表”。
- 普通网页查岗恢复“微信聊天 → 朋友圈 → 抖音 → X → 钱包 → 购物 → 浏览器 → 线下记录”的顶部逐项横幅；明确真实 iPhone 数据请求仍走原生读取。APP 共用入口，已同步避免误分流。
- 本机安全快照滚动到当前保存时间；较旧但更完整的副本作为历史完整快照保留，恢复页面不再因为旧档聊天更多而一直停在 8 月 9 日。
- 保留 v893 的 Xcode 编译修正：跨文件推送登记访问级别、iOS 26 Family Controls 分支、通知扩展 Framework 链接、MapKit 反查和角标 API 调整。

真机首次打开后，请允许通知、健康、定位和屏幕使用时间相关授权，再依次验证：角色明确读取时顶部是否逐项显示、数值是否与情侣空间真实面板一致、普通网页查岗是否显示微信／朋友圈／抖音步骤、手动解锁是否只提醒一次、后台或强退后的消息与来电通知是否到达。

Windows 自动化和工程静态检查不能代替 Mac 五 Target 的真实编译、签名和真机权限验收。如果 Xcode 仍有红色错误，请展开最下层错误并保留完整文字或截图，不要只截 `Command Ld failed` 顶层一句。
