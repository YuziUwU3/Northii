# 第二十四次安装说明

版本：网页核心 v899／私人 iOS 1.0.24 (24)。

请直接打开本交付目录中的 `PhoneCompanionTest.xcodeproj`，不要再覆盖回旧目录后编译。

本次修复：

- 保留顶部“正在读取什么”的逐项横幅。
- 读取期间模型输出被门禁拦截时保持静默，不再显示“模型没有生成／再点一次”。
- 读取完成后只把真实结果交给角色模型；由角色自己决定内容、语气、条数和分段，不使用固定系统汇总。
- 线下回应失败会写清余额／额度、密钥、权限、限流、配置、网络超时或空内容等原因；失败不修改原对话。

Windows 已完成 478 项自动测试。Mac 仍需编译五个 Target，并用真机验证微信聊天、电话、共同生活和线下失败提示。
