# 私人「小手机」App 唯一总纲

状态：已确认，2026-08-11 起生效。

本文是公开 North、私人「小手机」和网页版后续工作的最高级产品与技术约束。旧文档中“私人版名称未定”“继续用公开 North 控制本人设备”等内容全部失效；发生冲突时以本文为准。

## 1. 最终形态

- 公开版继续叫 **North**，供其他用户使用；当前审核中的版本保持不动。
- 私人版固定叫 **小手机**，只安装到本人设备；沿用现有猫咪图标，只修改 App 名称。
- 私人版是完整、独立的真实 iOS App，不是网页链接、PWA 或只打开远程网址的壳。
- 网页版继续保留，供安卓和浏览器使用。
- 三个入口共用后端、数据结构和主要业务代码。网页业务资源只有一份源码；私人 iOS App 在构建时把同一份资源打包进安装包，不复制维护第二套业务逻辑。
- 私人 App 的界面和普通功能本地可打开；AI、云同步、跨设备数据等需要联网的能力仍按功能需要联网。

## 2. 控制权隔离

- 公开 North 和私人「小手机」绝不能同时控制同一台真实 iPhone。
- 本人设备最终只由私人「小手机」持有 Family Controls、Device Activity 和 Managed Settings 控制权。
- 私人版启用前必须完成控制权迁移：停止公开 North 对本人设备的控制、废止其服务端控制凭据、确认私人版获得授权后再启用自动控制。
- 后端必须保存明确的 `controllerKind`、控制器实例 ID、租约版本和更新时间；旧控制器的命令一律拒绝，不能只靠界面隐藏来实现隔离。
- 同一条命令必须有稳定幂等键；重试只补做未完成步骤，不能重复执行。

## 3. 不可违背的验收标准

### 永久锁定

- 锁定不能因刷新、同步、重连、后台唤醒、快照更新、跨日、重新打开 App 或限额重算而自行解除。
- 仅两种操作可以解锁：角色明确发出解锁命令且收到真实 iPhone 执行回执；本人点击手动解锁。
- 达到每日限额可以自动锁定；跨日只重算用量，不能自动清除锁定。
- 收到真实执行回执前，不得显示“锁定成功”或“解锁成功”。

### 使用时间

- 昨天和今天严格分开；总屏幕时间、逐 App 时间和限额进度必须使用同一使用日数据。
- 快照同时保存使用日、采集时间、上传时间、时区、递增序号和来源设备。
- 旧快照不能覆盖新快照；页面刷新不能把已锁状态恢复成旧的未锁状态。

### 同步与回执

- 前台立即同步，后台被系统唤醒后自动同步，失败自动重试。
- 页面明确区分“命令已发送”“设备已收到”“设备已执行”；失败也必须显示真实失败阶段。
- 快照较旧只作提示，不能阻止发送锁定命令。
- 网络中断不能改写本地锁定账本，也不能把未知状态当作解锁。

## 4. 必须保留的旧功能

- 线上与线下共同生活只能有一边查手机和发消息。
- 线上线下共享查岗去重；每日必查总屏幕时间、逐 App 时间、睡眠、步数。
- 线下角色可口头锁定、解锁、修改限额，并等待真实设备回执。
- 普通共同生活对话继续使用用户设置的主模型；副模型只重试失败的当前一轮，下一轮恢复主模型；切换时显示三秒提示。
- “旁白”按钮只切换下方输入框模式，不弹中央输入框。
- 已稳定的跨渠道真实时间轴、说话人身份、上一句锚点和各独立 App 安全区不得回退。

## 5. 私人 iOS App 技术边界

- 主 App：SwiftUI 容器、本地打包的网页核心、原生桥、账号恢复、Keychain、持久缓存和同步状态。
- 原生能力：Family Controls 授权与选择、Device Activity Monitor/Report、Managed Settings Shield、后台通知与命令执行。
- 网页核心：现有聊天、共同生活、各内置 App 和设置；通过版本化原生桥请求真实设备能力。
- 数据原则：本地锁定账本优先保证安全，云端是跨设备主数据；两者合并时只能保留或加强锁定，不能由旧云快照解除锁定。
- 2026-08-11 已从用户提供的 `AppleProjects.zip` 取得真实 Mac 工程，确认五个 Target、Team、Bundle ID、App Group、entitlements 和现有 MapKit 实现。私人副本沿用已经签名可用的标识，只修改显示名称；审核工程本身保持不动。
- 崩溃日志中的 MapKit/VectorKit 后台看门狗已命中真实 `ContentView.swift`：授权定位后，SwiftUI `Map` 在 scene 进入后台时仍常驻。私人副本在 `.inactive` 阶段即移除地图视图，定位采集继续独立运行；必须通过真机前后台回归后才算修复完成。
- v881 的旧 8 秒竞速使用结构化 `withTaskGroup`，退出作用域仍会等待不响应取消的 DeviceActivity 子任务，不能真正解除“同步中”。私人副本改用不等待输家的 `AsyncStream` 竞速，并在晚到任务写状态前检查取消，禁止超时结果覆盖新快照。
- 2026-08-11 第一次真机安装已成功覆盖同 Bundle ID 的旧伴生包，原生屏幕时间页面和既有本地数据仍在；私人主界面白屏的 Xcode 真实日志为 `url is not inside resource directory url`、`outside the sandbox`。根因是 WKWebView 主文件 URL 与授予读取权限的目录没有按同一标准化路径生成。修复后构建脚本从共享 `小手机.html` 额外生成只作入口的 `PhoneWeb.bundle/index.html`，原生端从该入口推导唯一父目录并传给 `allowingReadAccessTo`；加载失败必须显示诊断页，禁止再用无提示白屏掩盖资源错误。
- 用户现有 `North备份_2026-08-10.json` 已只读验证：JSON 完整，约 4.5 MB、55 个顶层数据块。正式导入必须等私人主页面成功加载后再执行，导入前保留原文件并校验，不能拿空白新容器覆盖此备份。
- 私人 1.0.5 (5) 候选把话筒转字幕接入 iOS Speech/AVAudioEngine 原生桥，按原生键盘 frame 与 visualViewport 的差值修复通话和音乐输入栏，清除共享推送造成的固定假角标，并把真实设备管理入口限制在设置页。公开 North 仍保持 v881；这些候选能力必须经过真机授权、转写、键盘和角标验收后才能称为完成。
- v884／私人 1.0.8 的真机结果证明，单纯把输入框固定到底部、在角色语音前后 pause/resume 仍不足以解决问题。WKWebView 弹出键盘时会自动滚动最外层 UIScrollView，CSS 无法阻止整个通话页面上下跳；WebKit 语音播放完成回调又可能早于 AVAudioSession 真正释放，旧引擎即使返回“恢复成功”也可能已经失去输入。
- v885／私人 1.0.9 改用原生层处理这两个根因：仅在电话输入框聚焦期间锁定 WKWebView 外层滚动位置，电话输入改为微信式“点开输入—发送后收起—手动收起”的两态编辑器；角色语音结束后等待音频路由释放，再无条件清理并新建 AVAudioEngine、识别请求和任务。网页/PWA 已由用户确认恢复，本轮不再改其布局和话筒行为。
- 强制规则：私人 App 的键盘跳动不得再靠反复修改 CSS bottom 数值处理；角色播放后的连续录音不得仅调用 resume 或依赖 JS active 状态。必须分别以原生滚动锁和完整音频栈重建处理，并在真机连续完成至少三轮“用户说—角色说—用户再说”后才可判定通过。

## 6. 当前实施顺序

### 2026-08-11 v886／私人 1.0.10 通话键盘根因修正

- 真机复验结论：v885 的原生语音完整重建已经解决连续语音失效，必须保留；但 v885 的原生固定输入框、`contentOffset` 观察与滚动锁并没有解决键盘跳动，反而与 WKWebView 自带的键盘页面位移形成两套布局权威。
- 已确认对照基准：网页版／PWA 在同一通话页上表现正常，键盘出现时由浏览器把整个页面平滑上移；输入框位于挂断按钮上方，收起键盘后整体自然回位。
- v886 删除私人 App 独有的电话输入框分支、两态固定编辑器、焦点桥消息、`contentOffset` KVO 与强制滚动回写；原生 App 直接复用网页版同一份 `.callinput` 和 `.callbtns` 结构、位置及系统键盘行为。
- 强制规则：电话页只能有一个键盘布局权威。不得再为 `north-native-app` 单独覆盖 `.callinput`／`.callbtns`，不得监听并回写 WKWebView 外层滚动位置，不得用另一套原生两态输入框模拟网页输入框。
- 不可回退：原生语音在角色音频结束后延迟释放并完整重建 AVAudioEngine、识别请求和任务的修复继续保留；布局修复不得改回旧的 `resume` 假恢复。
- 验收要求：自动化 447/447 通过只说明回归契约成立；仍需真机连续多次打开键盘、发送、收起，确认整个页面与网页版同样平滑移动、没有上下闪跳，且挂断按钮始终在输入框下方，再连续完成至少三轮“用户说—角色说—用户再说”。

### 2026-08-11 私人 1.0.11 原生外框固定修正

- 真机否定结果：私人 1.0.10 虽删除了网页层第二套输入框与滚动锁，但开合键盘仍会先下拉再上弹，说明 v886 对根因只处理了一半。
- 新根因：`SmallPhonePrivateRootView` 只忽略 `.container` 底部安全区，没有忽略 `.keyboard` 安全区。键盘出现时 SwiftUI 会先缩小整个 `UIViewRepresentable`／WKWebView 外框，随后 WebKit 又为聚焦输入框执行自己的可见性处理，形成两次方向相反的页面动画。
- 修正：私人 1.0.11 在原生根视图增加 `.ignoresSafeArea(.keyboard, edges: .bottom)`，固定 WKWebView 外框尺寸；网页核心仍为 v886，不再改网页版 CSS，也不回退已经修好的原生连续语音重建。
- 强制规则：以后遇到原生键盘导致整页跳动，必须先检查 SwiftUI 根容器是否响应键盘安全区；不得先改网页 `bottom`、不得恢复 `contentOffset` KVO，也不得把已正常的网页版一起改坏。

### 2026-08-11 v887／私人 1.0.12 手机号账号与云恢复

- 手机号入口只在私人「小手机」App 的「设置 → 授权·数据」出现；公开 North 与网页版不显示、不持有该登录态。
- 验证码通过 Supabase Auth Phone OTP；访问令牌与刷新令牌只存 iOS Keychain，不交给网页 JavaScript。
- 云备份表按 `auth.uid()` 开启 RLS，一名用户只能读写自己的备份；上传同时保存数据采集时间、上传时间、版本、大小和校验值，旧采集时间禁止覆盖新备份。
- 首次绑定时：云端为空才自动上传当前本机；云端已有数据则必须由用户明确选择保留本机或恢复云端，绝不静默覆盖已经导入的数据。
- 当前上线前置条件：Supabase 后台启用 Phone Provider 并配置短信服务，然后执行 `supabase/migrations/202608110001_private_phone_accounts.sql`。未完成前，App 必须明确显示账号服务不可用，不能伪装成绑定成功。

### 2026-08-11 v888／私人 1.0.13 中国大陆手机号密码登录

- 用户确认最终目标不变：私人 App 仍以手机号作为可见账号，删除或重装后输入手机号和密码恢复云端数据；底层实现不要求必须发送短信验证码。
- v887 的 Phone OTP 方案不再作为当前实施方向。Supabase 托管 Phone Auth 必须另配短信供应商，而中国大陆私人号码没有现成供应商凭据，继续依赖 Twilio 会让登录入口在真实环境不可用。不得为了保持“验证码”形式而让用户购买或配置未经真机验证的境外短信链路。
- v888 改为 Supabase Email/Password 登录，但 App 界面不暴露邮箱：原生桥先把中国大陆号码规范化为 `+86`，再确定性映射为 `smallphone.86手机号@example.com`。该内部邮箱只作 Auth 标识，公开 North 与网页版均不得显示或使用这套私人登录态。
- 密码只随当前登录请求发给 Supabase Auth，不写入网页状态、本地文件、日志、UserDefaults 或 Keychain；Keychain 只保存访问令牌、刷新令牌、到期时间、用户 ID 和用于遮罩显示的手机号。
- Phone Provider、Twilio 和短信服务保持关闭。首次启用只需执行 `supabase/migrations/202608110001_private_phone_accounts.sql`，并由本人在 Authentication 中手动创建一名已确认的内部邮箱用户；禁止开放公共自助注册。
- 云备份表、`auth.uid()` RLS、采集时间优先、旧备份不得覆盖新备份、首次登录必须明确选择保留本机或恢复云端等安全规则全部不变。
- 回归测试必须明确拒绝旧 `/auth/v1/otp`、`account.otp.send` 和 `account.otp.verify` 契约，并验证密码 grant、内部邮箱映射、Keychain 不含密码以及公开 North／网页版不出现私人账号入口。

1. 固化本文和私人 App 源码骨架，建立本地资源打包与原生桥契约。
2. 已取得并导入 Mac 完整工程，已核对 Target、扩展、签名、App Group 和 MapKit 实现。
3. 正在把现有伴生能力与本地网页核心合入私人主 App，并修复真实超时和后台地图问题。
4. 增加服务端控制器租约，关闭公开 North 对本人设备的控制入口。
5. 接入手机号稳定账号、Keychain、本地缓存、云端恢复与迁移。
6. 完整回归共同生活、查岗、模型、旁白、时间轴和安全区。
7. 进行真机跨日、断网、后台、刷新、重复命令、旧快照和长时间稳定性测试。
8. 更新维护资料、跑全量测试、版本加一、提交并普通推送。

## 7. 完成定义

只有当私人「小手机」可在真机独立安装、本人设备只受它控制、所有关键状态均有真实设备回执，并通过第 6 节的真机测试后，才可称为“私人 App 完成”。仅有源码骨架、网页壳或模拟器通过都不算完成。

## 9. 2026-08-11 v891／私人 1.0.16 外置实时权限与一键全查

- 角色既可查看屏幕时间、睡眠、步数、心率、电量或定位中的单项，也可口头要求“一键查看全部数据”。全部查看必须依次显示正在查看屏幕时间与逐 App、睡眠与步数、心率、电量、实时定位，全部取得本次外置快照后才允许回复。
- 角色任何查看都只使用本次外置 iPhone 新快照；不得读取、复述或兜底使用情侣空间伴生缓存。设备未回传或 revision 未推进时必须如实失败。
- 原生 App 的本地网页定位由文档启动时注入的 `location.current` 原生桥接接管，同时覆盖 `Permissions API` 查询，禁止 WebKit 再以 `file://.../PhoneWeb.bundle/index.html` 身份弹出第二层网站定位授权。
- Family Controls 仅在系统状态为 `notDetermined` 时请求一次；已有授权时直接读取。定位在 App 启动、回到前台与系统后台唤醒后自动恢复；持续后台定位仍必须由用户在 iOS 设置中一次性授予“始终”和精确位置，代码不得绕过系统权限。
- 数据必须区分设备采集时间与云端上传时间；旧快照不得覆盖新快照，刷新、重连、跨日与限额重算不得解除已确认锁定。
- 禁止破坏已经稳定的原生话筒、电话/微信键盘、页面固定、安全区和网页兼容行为。

## 8. 2026-08-11 v890／私人 1.0.15 唯一控制器与外置直读

- 根因：从浏览器或旧 App 导入的完整业务备份包含伴生设备连接状态与小手机 ID，但外置控制密钥只保存在原入口的 localStorage，未进入备份。私人 App 会在自己的文件域生成新密钥，因此页面可显示“已连接”，服务器却拒绝锁定、同步和查看命令。该问题在手机号登录前已经存在，手机号账号不是故障原因。
- 唯一控制器：已登录的私人小手机通过 Keychain 中的稳定实例 ID 和手机号账号 JWT 调用受保护 RPC，核对该账号云备份中的小手机 ID 后轮换外置 owner 密钥。真实 iPhone 的 device 密钥、配对关系和永久锁定账本不得改变。密钥轮换后，公开 North 的旧控制密钥失效，不能再控制本人设备。
- 唯一数据源：角色查看屏幕时间、逐 App、睡眠、步数、心率、电量和位置时，真实 iPhone 最新快照是唯一事实来源。情侣空间只负责权限、入口、命令状态和本地显示缓存，不得成为第二套采集数据，也不得用缓存替代本次外置拉取。
- 查看流程：角色查看前先拉取服务器最新外置快照；若对应数据超过新鲜度，自动提交 `view` 同步命令并唤醒伴生 App，只有收到采集时间更新的新快照后才继续。超时则本轮不向角色提供旧数据。
- 时间语义：屏幕使用结果必须同时带使用日、设备采集时间和云端上传时间。上传较晚不能把旧使用日或旧 revision 伪装成新数据，刷新也不能覆盖永久锁定账本。
- 禁止回退：不得重新让公开 North 与私人小手机同时持有有效 owner 密钥；不得把 `S.couple.companion` 中的显示缓存描述成独立数据源；不得在未收到真实设备新快照时让角色基于旧值作出反应。

## 10. 2026-08-11 v892／私人 1.0.17 本机直连、全天主动联系与通信通知

- 产品形态统一：装进私人“小手机”App 的网页核心就是唯一主界面，原生层是同一个 App 的设备能力，不再向用户呈现“内置/外置”两个产品模式。本机数据和控制直接走原生桥；只有角色需要控制另一台伴生 iPhone 时，才走云端远程链路。
- 本机真实读取：原生桥契约升级为 10，网页通过 `device.snapshot` 读取屏幕使用时间、健康、定位、电量和锁定账本等当前 iPhone 快照。页面不得用情侣空间缓存、演示数据或旧云快照冒充本次本机读取。
- 本机真实控制：锁定、解锁和修改限额通过 `device.command` 直接交给 Managed Settings/Family Controls 执行。只有原生回执 `stage = executed` 才能向角色和用户显示成功；`queued`、`sent` 或无回执都不等于执行成功。永久锁定账本继续优先，刷新、重连、跨日与旧快照不得自动解锁。
- 主动联系统一调度：共同生活模式允许全天消息和来电，但必须在最近一次会话互动结束后随机静默 30–60 分钟，不能固定为 30 分钟，也不能在正在聊天、正在通话或线下场景中插入主动联系。后台设置的“主动联系间隔分钟”为 0 时，不启用固定间隔，只按每日随机条数分布；填写正数时，只按该固定分钟间隔并仍受每日上限约束，两套调度器不得同时触发。
- 语境与防复读：用户说去忙时，角色可询问“忙得怎么样”或分享自己的日常，但不得责问为什么没回复；用户说睡觉、午睡或给出睡眠时长后，在预计醒来前禁止主动消息和主动来电。主动消息和主动电话都必须作为新发生的生活事件生成，不得重新回答、改写或复述用户上一句；发送前继续做语义重复拦截。
- 后台通知：服务端对每条主动消息分别发送 APNs alert，带默认提示音和通信通知字段；通知服务扩展使用角色头像构造通信发送者，用户点击后进入对应聊天或来电页。iOS 顶层系统通知仍可能显示 App 图标，这是系统边界，不能承诺完全替换成角色头像；当前“主动来电”是有声音的 APNs 来电提醒并在点击后进入 App 内电话，不冒充持续响铃的 CallKit/VoIP 真来电。
- 服务端前置：必须应用 `supabase/migrations/202608110003_phone_role_push_all_day_random_idle.sql` 并重新部署 `supabase/functions/phone-role-push/index.ts`，否则强制退出 App 后的全天主动联系、30–60 分钟静默和睡眠安静期不会完整生效。
- 自动化证据：Windows 上 `node --check app.js` 通过，`node --test tests/*.test.mjs` 共 458/458 通过；这些证据只覆盖网页、契约和静态原生集成，不代替 Xcode 五 Target 编译、签名、通知扩展、后台/强退 APNs、Family Controls 与 Managed Settings 真机执行测试。
- 当前结论：源码候选已形成，但在 Mac 编译并完成真机锁定、解锁、读取、前后台/强退通知和 30–60 分钟主动联系回归前，不得把 v892／1.0.17 写成“真机已经全部完成”。

## 11. 2026-08-11 v893／私人 1.0.18 逐项真实读取、明确解锁事件与后台自登记

- 本次错误证据：角色报告 78 步、睡眠 0、抖音 120 分钟，但同一时刻真实面板显示 497 步、睡眠 9 小时 46 分钟。根因是角色事实同时暴露 `usedSec` 和 `limitMin`，模型把 120 分钟限额当成真实用量；通用上下文还可能混入旧伴生缓存，位置无权限时会回退剧情位置。
- 单次读取隔离：原生桥契约升级为 11。每次 `device.snapshot` 必须返回 `readSessionId`、`requestedFocus` 和 `capturedAt`；角色只能消费该次原生快照。屏幕事实只包含实际用量，不包含限额；位置没有真实授权就返回空，不得使用 `curLoc()` 或旧剧情位置。
- 可见进度与超时：一键读取依次显示电量、总屏幕使用、每个真实 App 的软件使用时间、步数、睡眠、最新心率、心电/HRV 和位置。JS→Swift 快照 25 秒超时，健康读取 12 秒超时；前台角色读取不再等待最多 8 次地址反查。部分权限失败时返回其余真实项目并列出不可用项。
- 数值守卫：模型回答出现本次快照没有的数字，或在事实没有限额时引入“限额/卡着 120 分钟”，必须替换为确定性真实摘要。该规则同时覆盖在线查手机和共同生活现场查看。健康摘要新增最新 ECG 分类、平均心率、症状状态、测量数和来源概要；仍不作医疗诊断。
- 手动解锁事件：提醒改为必开。原生设备管理页亲自解锁和 owner 控制页收到 `stage = executed` 后解锁都会生成 `explicit manualUnlock`；角色自己下达的解锁不生成。事件跨快照合并并保留 `delivered`，只有角色成功发出一次反应后才完成，普通锁定差异不得制造解锁权威。
- 后台主动联系根因：1.0.17 本机直连绕过旧配对是正确的，但 APNs 令牌登记仍要求 `CompanionSyncService.isPaired`。因此页面可显示本机已连接，服务器却未必有统一 App 的 device secret 和 APNs token，后台开关可能保存失败或没有通知。
- 后台自登记修复：新增 `202608110004_private_phone_unified_push.sql`。手机号账号核验云备份中的 cloudId 后，统一 App 同时登记 owner/device 身份、Keychain 设备密钥和 APNs token；无需第二个伴生页面。`phone_role_push_status` 返回 token、profile、cron、nextDue 和上次推送状态，角色资料页直接显示是否真正接通，profile 每 10 分钟自愈同步。
- 时间解释不变：主动联系间隔填写 1 分钟，不代表聊天后 1 分钟立即发消息；最近一次会话后仍先随机静默 30–60 分钟，1 分钟只约束静默期结束后的后续联系。睡眠、正在聊天、通话和线下场景仍禁止插入。
- 服务端前置：必须顺序应用迁移 003 和 004，重新部署 `phone-role-push`，并确认 Edge Function 的 APNs 参数和每分钟 cron。缺少任一项时，设置页应明确显示链路未接通，不能只亮开关。
- 自动化证据：Windows 上 `node --check app.js` 和 `git diff --check` 通过，完整自动化 463/463 通过。Mac 五 Target 编译、HealthKit ECG API、Family Controls/Managed Settings 真机执行、通知服务扩展以及前台/后台/强退 APNs 仍待验证；不得把 v893／1.0.18 写成真机全部完成。

## 12. 2026-08-11 v893／私人 1.0.18 Xcode 编译修正 1

- Mac 首次编译发现唯一红色阻塞项：`PhoneNativeBridge.swift` 跨文件调用 `registerPushTokenIfAvailable`，但该方法在 `CompanionSyncView.swift` 中误用了仅限本文件的 `fileprivate`。
- 已改为默认的模块内访问级别，并增加声明端与调用端回归断言。业务逻辑、网页 v893、iOS 1.0.18 (18) 和原生桥契约 11 均不变。
- Xcode 截图中的 iOS 26 地理编码弃用、iOS 17 角标弃用、Family Controls Development 和 actor isolation 项目均为黄色警告，不是本轮红色编译错误。Mac 仍须使用编译修正版重新编译，Windows 测试不能替代该结果。

## 13. 2026-08-11 v893／私人 1.0.18 Xcode 编译修正 2

- 第二张 Mac 截图仍显示已经从仓库和第一修正版删除的 `fileprivate` 红错，证明 Mac 仍在编译旧目录或新旧源码发生混编。后续包必须以 `SmallPhone_v893_CompileFix2` 全新独立工程目录交付，直接打开其中的 xcodeproj，禁止再覆盖到旧 `XcodeProject`。
- `ContentView` 已显式覆盖 iOS 26 的 `approvedWithDataAccess` 分支；`RoleNotificationService` Target 已显式链接 `Intents.framework` 和 `UserNotifications.framework`。定位反查迁移到 `MKReverseGeocodingRequest`，角标清理移除弃用的 `applicationIconBadgeNumber` 回退。
- 静态验收必须检查 PBX 对象引用、五个 Target、Swift 源文件唯一性、全部 plist/entitlements、跨文件访问级别和通知扩展 Frameworks。`Command Ld failed` 的截图没有展开具体链接器正文，因此 Mac 新目录重新编译仍是最后边界；若继续失败必须取得下一层完整日志，禁止凭顶层一句虚构根因。

## 14. 2026-08-11 v894／私人 1.0.19 真实查看触发、网页横幅与滚动恢复快照

- 角色真实读取触发补齐电量、总屏幕、逐 App、步数、睡眠、心率、心电／HRV 和位置。任何回复若在没有本次读取 ID 时声称“没刷新、没同步、没戴手表、读取不到”，会先被改为实际读取；低电量等自动化只能谈本次授权类别，不能顺带编造屏幕或健康状态。
- 角色一键与明确单项读取必须显示当前读取项目。普通网页版查岗不属于外置真实读取：没有结构化 `intent` 时直接执行微信聊天、朋友圈、抖音、X、钱包、购物、浏览器和线下记录的经典顶部逐项横幅；只有明确真实数据请求才调用外置 iPhone。APP 共用入口，已同步避免同类误分流。
- 8 月 9 日恢复日期冻结的根因不是时区，而是旧快照完整度分数较高时拒绝所有新写入，候选又先按分数排序。v894 将 `__recovery_state` 改为最新滚动副本，把较旧但更丰富的内容归档为 `__recovery_history_state`，只拒绝 `savedAt` 倒退的乱序写入；恢复候选先按保存时间，再按完整度排序。
- 网页发布范围严格限于经典查岗横幅和恢复快照修正，没有把原生锁定、Family Controls、HealthKit、真实位置或 APNs 功能塞入网页。网页与 APP 继续共享核心，但真实能力仍由原生桥和授权决定。
- 当前版本为网页 v894、私人 iOS 1.0.19 (19)、桥契约 11。Windows 自动化、静态 Xcode 工程校验和安装包结构检查完成后方可交付；Mac 五 Target 编译、签名、真实 HealthKit／Screen Time／位置读取以及前台、后台、强退通知仍必须真机验证。

## 15. 2026-08-11 v895／私人 1.0.20 全量读取、后台链路与记忆清空

- 电量和位置能成功并不代表统一读取链路成功：电量直接来自 `UIDevice`，不需要权限；位置来自独立 CoreLocation 授权。健康数据此前只打开了网页角色权限，未同步打开原生 `healthSyncEnabled`，所以角色“读取全部”不会真正请求 HealthKit。1.0.20 在明确读取健康类别时启用并刷新 HealthKit。
- 屏幕时间直接数据 API 要求 `approvedWithDataAccess`，正式用户安装又受欧盟设备和欧盟 Apple Account 限制；中国真机通常只能取得普通 `approved`。1.0.20 不再把普通授权当作失败，而是挂载既有 `DeviceActivityReport`，由 Report 扩展把当日总时长和逐 App 数据写回 App Group，并以本次请求编号读取，避免拿旧快照冒充。
- “读取全部”最终答复固定逐项列出电量、总屏幕、逐 App、步数、睡眠、心率、心电、HRV 和位置；每项必须是本次真实值或明确失败原因。App 专属分支必须同时满足原生桥可用，网页端不会被导入真实设备能力。
- 主动消息失败的真实原因是远端数据库缺少本地已有的 003／004 迁移：页面报错的统一控制器 RPC 实际不存在，全天随机调度函数仍是旧签名。已部署 003–006、补上旧 `p_apns_env` 参数兼容、服务端记忆清空 RPC，并重新部署 `phone-role-push`；cron 仍按分钟运行。
- 彻底清空旧记忆此前只清了当前联系人对象，没有等待大聊天归档覆写，也没有清共同生活 home，更没有清服务端 `recent_context`／`memory_context` 和未消费 outbox。v895 改为等待本地归档和服务端清空都完成；任一步失败都明确提示“未彻底完成”，不会假报成功。
- 网页大容量存档失败源于 IndexedDB 已是 v1 但缺少对象仓库时不会再次触发升级，且写入一次失败便终止。v895 升到 v2，确保仓库存在并进行三次短退避重试。网页只获得存储和记忆正确性修复，不获得 App 锁定、HealthKit、Screen Time 或 APNs。
- Windows 472 项测试已通过。Mac 五 Target 编译、签名，以及 HealthKit 首次授权、Report 扩展回传、前台／后台／强退通知头像和主动来电仍必须真机验收。
